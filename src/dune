
(library
  (name Phabrador)
  (libraries str flex belt RexJson fluid.Files fluid.MacOS odate)
  (c_names Phabrador)
  (flags :standard -open RexJson -w -A)
    (c_flags -x objective-c -g)
  (preprocess (pps belt.ppx fluid.ppx let-anything-esy.ppx)))

